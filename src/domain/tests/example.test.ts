import assert from 'node:assert'
import { describe, test } from 'node:test'

describe('testing example', () => {
  test('verify true value', () => {
    assert.equal(true, true, 'True should be true ou')
  })
})
